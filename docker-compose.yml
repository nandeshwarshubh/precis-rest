services:
  postgres:
    build:
      context: .
      dockerfile: Dockerfile.postgres
    container_name: precis-postgres
    command: postgres
    environment:
      POSTGRES_DB: precis
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      # PostgreSQL 18+ requires mounting /var/lib/postgresql instead of /var/lib/postgresql/data
      # This allows pg_upgrade to work properly with --link option
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d precis"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - precis-network

  zipkin:
    image: openzipkin/zipkin:latest
    container_name: precis-zipkin
    ports:
      - "9411:9411"
    environment:
      - STORAGE_TYPE=mem
    networks:
      - precis-network

  app:
    build:
      context: .
      dockerfile: Dockerfile.app
    container_name: precis-app
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=jdbc:postgresql://postgres:5432/precis
      - DATABASE_USERNAME=postgres
      - DATABASE_PASSWORD=postgres
      - ZIPKIN_ENDPOINT=http://zipkin:9411/api/v2/spans
      - SPRING_PROFILES_ACTIVE=prod
      - CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost:4000
    depends_on:
      postgres:
        condition: service_healthy
      zipkin:
        condition: service_started
    networks:
      - precis-network

volumes:
  postgres_data:

networks:
  precis-network:
    driver: bridge

